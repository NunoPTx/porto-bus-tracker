name: Hourly Bus Check
on:
  schedule:
    - cron: '*/5 * * * *' # This runs it every hour on the hour
  workflow_dispatch: # This lets you run it manually to test

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: write  # <--- THIS IS REQUIRED
    steps:
      - name: Checkout code
        uses: actions/checkout@v4 # Updated from v2 to v4
      - name: Set up Python
        uses: actions/setup-python@v5 # Updated from v2 to v5
        with:
          python-version: '3.9'
      - name: Install dependencies
        run: pip install requests
      - name: Run script
        run: python tracker.py
      - name: Save results
        run: |
          git config --global user.name 'BusTrackerBot'
          git config --global user.email 'bot@noreply.github.com'
          git add roster.json
          git commit -m "Update bus roster" || exit 0
          git push
